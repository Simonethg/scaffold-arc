# Capturas del playground

Archivos actuales:

- `playground-usdc-desktop.png` — 1440×900 (full page)
- `playground-usdc-mobile.png` — 390×844 (full page)

Regenerar (con `yarn nextjs:dev` en otra terminal):

```bash
npx playwright install chromium   # una vez
yarn screenshots
```

Referenciadas desde el README raíz.
